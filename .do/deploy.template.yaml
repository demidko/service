spec:
  name: service
  services:
    - name: backend
      github:
        repo: demidko/service
        branch: main
        deploy_on_push: true
      dockerfile_path: Dockerfile
      envs:
        - key: TEXT_PARAM
          value: "password-testvalue"
          type: SECRET