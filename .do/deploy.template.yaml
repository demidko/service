spec:
  name: service
  services:
    - name: backend
      git:
        repo_clone_url: https://github.com/demidko/service
        branch: main
      envs:
        - key: TEXT_PARAM
          type: SECRET
