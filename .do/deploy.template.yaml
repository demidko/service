spec:
  name: service
  services:
    - name: backend
      git:
        repo_clone_url: https://github.com/demidko/service
        branch: main
        deploy_on_push: true
      envs:
        - key: TEXT_PARAM
          type: SECRET
