spec:
  name: service
  services:
    - name: backend
      dockerfile_path: Dockerfile
      github:
        repo: demidko/service
        branch: main
        deploy_on_push: true
      envs:
        - key: EXAMPLE
          type: SECRET
